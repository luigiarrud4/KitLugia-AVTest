<Page x:Class="KitLugia.WinPE.Pages.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BgDarkBrush}"
      Title="Dashboard">
    <ScrollViewer Padding="24">
        <StackPanel>
            <TextBlock Text="Dashboard" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="Status do ambiente WinPE" Foreground="{StaticResource TextSecondaryBrush}" Margin="0 4 0 24"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0 0 8 0">
                    <StackPanel>
                        <TextBlock Text="Ambiente" Style="{StaticResource SectionTitle}"/>
                        <TextBlock x:Name="EnvValue" FontSize="18" FontWeight="Bold" Margin="0 8 0 0"/>
                        <TextBlock x:Name="BootMode" Foreground="{StaticResource TextSecondaryBrush}" Margin="0 4 0 0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardBorder}" Margin="8 0">
                    <StackPanel>
                        <TextBlock Text="Unidades Detectadas" Style="{StaticResource SectionTitle}"/>
                        <TextBlock x:Name="DriveCount" FontSize="18" FontWeight="Bold" Margin="0 8 0 0"/>
                        <TextBlock x:Name="DriveDetails" Foreground="{StaticResource TextSecondaryBrush}" Margin="0 4 0 0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardBorder}" Margin="8 0 0 0">
                    <StackPanel>
                        <TextBlock Text="WinXShell" Style="{StaticResource SectionTitle}"/>
                        <TextBlock x:Name="ShellValue" FontSize="18" FontWeight="Bold" Margin="0 8 0 0"/>
                        <TextBlock x:Name="ShellHint" Foreground="{StaticResource TextSecondaryBrush}" Margin="0 4 0 0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource CardBorder}" Margin="0 16 0 0" Padding="20">
                <StackPanel>
                    <TextBlock Text="Ferramentas RÃ¡pidas" Style="{StaticResource SectionTitle}" Margin="0 0 0 12"/>
                    <WrapPanel>
                        <Button Content="ðŸ“  Abrir Explorador" Style="{StaticResource AccentButton}" 
                                Click="OpenExplorer_Click" Margin="0 0 8 8"/>
                        <Button Content="ðŸ’¾  Gerenciar PartiÃ§Ãµes" Style="{StaticResource AccentButton}"
                                Click="OpenPartitions_Click" Margin="0 0 8 8"/>
                        <Button Content="ðŸ“¦  Shrink Inteligente" Style="{StaticResource AccentButton}"
                                Click="OpenShrink_Click" Margin="0 0 8 8"/>
                        <Button Content="ðŸ–¥ï¸  Instalar Windows" Style="{StaticResource AccentButton}"
                                Click="OpenInstall_Click" Margin="0 0 8 8"/>
                    </WrapPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
