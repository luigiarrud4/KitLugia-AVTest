<Window x:Class="KitLugia.GUI.Windows.UninstallHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Histórico de Desinstalações" Height="560" Width="760"
        WindowStartupLocation="CenterOwner"
        TextElement.FontFamily="Segoe UI Variable Display, Segoe UI, Consolas, Segoe UI Emoji"
        TextElement.Foreground="White"
        Background="#0F0F0F"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False" ResizeBorderThickness="4"/>
    </WindowChrome.WindowChrome>
    <Border BorderBrush="#FFD700" BorderThickness="1">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🕘 Histórico de Desinstalações" FontSize="15" FontWeight="SemiBold" Foreground="#FFD700"/>
                    <TextBlock x:Name="TxtCount" Text="" FontSize="11" Foreground="#666" Margin="10,3,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock x:Name="BtnClose" Text="&#x2715;" FontSize="12" Foreground="#666"
                           HorizontalAlignment="Right" VerticalAlignment="Center"
                           Cursor="Hand" Margin="0,0,4,0" MouseDown="BtnClose_MouseDown"/>
            </Grid>

            <!-- List -->
            <ListBox x:Name="HistoryList" Grid.Row="1" Margin="0,10,0,8"
                     Background="#0A0A0A" BorderBrush="#222" BorderThickness="1"
                     Foreground="#E6E6E6" FontSize="12"
                     SelectionMode="Single"
                     VirtualizingStackPanel.IsVirtualizing="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#151515" CornerRadius="5" BorderBrush="#2A2A2A" BorderThickness="1"
                                Padding="8,6" Margin="2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Center" Width="130">
                                    <TextBlock Text="{Binding Timestamp, StringFormat=dd/MM/yyyy HH:mm}" FontSize="11" Foreground="#888"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding AppName}" FontWeight="SemiBold" FontSize="13" Foreground="#EEE"/>
                                    <TextBlock Text="{Binding Summary}" FontSize="11" Foreground="#AAA"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Footer -->
            <Grid Grid.Row="2">
                <TextBlock x:Name="TxtDetail" FontSize="11" Foreground="#888" TextWrapping="Wrap"
                           VerticalAlignment="Center" Margin="0,0,10,0" MaxHeight="50"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnRestore" Content="Restaurar" Margin="0,0,8,0"
                            Background="#2F4D2F" BorderBrush="#4CAF50" IsEnabled="False" Click="BtnRestore_Click"/>
                    <Button x:Name="BtnDelete" Content="Excluir do histórico" Margin="0,0,8,0"
                            IsEnabled="False" Click="BtnDelete_Click"/>
                    <Button Content="Atualizar" Click="BtnRefresh_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>